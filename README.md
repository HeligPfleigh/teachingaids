# System required
  - Install docker or mongodb

# How to use
  - Development mode:
    `./deploy development`

  - Staging mode (run production mode on local):
    `./deploy staging`

  - Production Mode (env docker only):
    `./deploy production`
